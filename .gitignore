# macOS
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Dependency directories
node_modules/

# Output of 'npm pack'
*.tgz

# dotenv environment variables file
.env
.env.test

# Distribution
# NOTE: dist is committed on purpose — npm installs this repo from git and
# needs the built output present (npm's git-dep prepare step is unreliable
# for global installs). Rebuild with `pnpm build` and commit dist on changes.

# Eslint cache
.eslintcache

# Local pnpm state (approve-builds)
pnpm-workspace.yaml
